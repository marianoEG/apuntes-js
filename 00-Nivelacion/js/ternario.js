const cuenta = 10;

// let mensaje;

// if (cuenta < 0) mensaje = "no tene plata";
// else mensaje = "tene plata";

//OPERADOR TERNARIO
const mensaje = cuenta < 0 ? "No tiene saldo" : "Tiene saldo";

document.write(mensaje);
